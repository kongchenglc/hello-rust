mod base_syntax;
mod control_syntax;
mod fn_syntax;
mod guess_num;
mod owner_ship;
mod struct_syntax;

fn main() {
    // guess_num::start();
    // base_syntax::start();
    // fn_syntax::start();
    // control_syntax::start();
    // owner_ship::start();
    struct_syntax::start();
}
