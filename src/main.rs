mod guess_num;
mod base_syntax;
mod fn_syntax;
mod control_syntax;
mod owner_ship;

fn main() {
    // guess_num::start();
    // base_syntax::start();
    // fn_syntax::start();
    // control_syntax::start();
    owner_ship::start();
}
